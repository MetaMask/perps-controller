/**
 * Global type declarations for PerpsController.
 *
 * These types are provided by the host environment (React Native, browser, etc.)
 * but need declarations for standalone TypeScript compilation.
 */

/** React Native __DEV__ global (true in development builds) */
declare const __DEV__: boolean;
